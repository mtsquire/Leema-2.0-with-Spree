<% if @product.images.count > 0 %>
  <%= image_tag @product.images.first.attachment.url(:large), :itemprop => "image", :class => "img-responsive center-block" %>
<% else %>
  <%= image_tag("leema-product-placeholder.jpg", :class => "img-responsive center-block") %>
<% end %>
