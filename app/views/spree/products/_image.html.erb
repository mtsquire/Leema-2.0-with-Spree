<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), :itemprop => "image" %>
<% else %>
  <%= image_tag("leema-product-placeholder.jpg", :class => 'img-responsive center-block') %>
<% end %>
